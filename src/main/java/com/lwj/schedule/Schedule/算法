排班核心算法在GA_group.java中！
而GA_group.java中的排班概括：
    其实是在对GA_group.java中的group，status，check，workingtime四个数组进行各种操作
    最终的结果就是把每个人的排班结果放到group数组中，之后再进行解码

GA_group.java中：
    group表示所有员工的集合
    check是对当前状态的一种确认，无论是上班还是不上班，如果确定了之后都要把对应的check值设为1
    status是排班过程中对员工的状态确认的数组，最终要保证所有员工的每个时间片status都为1
    chromo就是排班结束后，表示每个员工在每个时间片是否上班，chromo为0则那个时间片不上班，1则上班
    workingtime包含了每个员工连续工作时间、日工作时间、周工作时间的记录


GA_group中：
    init（）后首先对check，status，workingtime做了初始化
    然后之后用love_arr进行check和status的修改


书签中的：
    阿拉伯数字——》我觉得需要修改的地方
    大写英文字母——》是我没明白，有疑问的地方